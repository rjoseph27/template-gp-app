/**
 * @constant
 * @description The key for the user id in the local storage
 */
export const USER_ID_LOCAL_STORAGE_KEY = 'userId';

/**
 * @constant
 * @description The key for the token in the local storage
 */
export const TOKEN_LOCAL_STORAGE_KEY = 'token';

/**
 * @constant
 * @description The key for the language in the local storage
 */
export const LANGUAGE_LOCAL_STORAGE_KEY = 'lang';
